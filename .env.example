# Banco de dados
POSTGRES_USER=price_monitor
POSTGRES_PASSWORD=troque_esta_senha
POSTGRES_DB=price_monitor

# Montado automaticamente no docker-compose, mas útil para rodar localmente sem Docker:
# DATABASE_URL=postgresql+psycopg2://price_monitor:troque_esta_senha@localhost:5432/price_monitor
